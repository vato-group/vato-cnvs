<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened Runtime entitlements pour Tauri 2 + WebKit. -->

  <!-- WebKit JS / WebAssembly utilise du JIT et de la mémoire exécutable non signée. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- Sans ça, certaines lib dynamiques chargées via dlopen (pty, natives) sont rejetées. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Accès réseau sortant (CLIs, updater). -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!-- Lecture/écriture de fichiers sélectionnés par l'utilisateur (open/save dialogs). -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
